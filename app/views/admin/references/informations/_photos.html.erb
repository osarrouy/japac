<%= link_to_remote 'Ajouter une photo', :url => new_admin_reference_photo_path(@reference, :js),
		:method => :get, :update => 'photos', :html => { :class => 'top_links' } -%>
		
<h3>Photos</h3>

<%= render :partial => 'admin/separators/separator_thick' %>

<div class="photos_list">
	<%= render :partial => 'admin/photos/edition_list', :object => @reference %>
</div>